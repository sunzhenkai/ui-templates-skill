# 服务管理 — workbench-shell web

## 概览

- 本地测试：在 `example/workbench-shell/web` 执行 `pnpm install`，再由 service-manager 启动 Vite 开发服务。
- 前置：Node.js 24、pnpm 9.15、已执行 `pnpm install`
- 项目根：`example/workbench-shell/web`

## 服务

### web

- **command**: `pnpm exec vite --host 0.0.0.0 --port 4173`
- **cwd**: `.`
- **port**: 4173
- **source**: package.json
- **health**: `http://127.0.0.1:4173/`

## Profiles

无。

## Notes

- 人工：测试服务必须监听 `0.0.0.0`，便于容器或局域网访问。
- Vite 热更新：是。
- 日志：`~/.cache/service-manager/logs/6d25465aa133942a38dfb42751abd645/web.log`
- 端口冲突时先运行 `ss -ltnp | rg ':4173'` 核对进程身份。
