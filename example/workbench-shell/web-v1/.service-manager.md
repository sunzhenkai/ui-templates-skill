# 服务管理 — web-v1

## 概览

- Vite + React 开发服务，消费 `workbench-shell` 模板的样例前端
- 前置：Node / pnpm；`pnpm install` 后即可启动

## 服务

### web-v1

- **command**: `pnpm exec vite --host 0.0.0.0 --port 5176`
- **cwd**: `.`
- **port**: 5176
- **source**: package.json
- **notes**: 开发/测试服务。绑定 `0.0.0.0`；热更新：是。默认 5173 常被本机其它 Vite 占用，故固定 5176。根脚本 `pnpm dev` 等价于 `vite`，启动时追加 `--host 0.0.0.0 --port 5176`。

## 踩坑

- 2026-09-04：本机 `5173`/`5174`/`5175` 已被其它项目 Vite 占用（含已删除目录的残留进程）→ 本服务使用 `5176`，不要按端口杀占用进程
