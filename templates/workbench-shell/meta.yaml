name: workbench-shell
description: 工作台/后台型 Web 应用 App Shell 布局规范:整页不滚动面板滚动、chrome 常量统一、1px 分割线分区、五种页面布局模式
source:
  type: doc
  ref: 用户提供的 Markdown 布局设计文档(原始项目出处已按要求抹除,业务实体已泛化)
captured_at: 2026-08-30
tokens: tokens.yaml
platforms: [web, mobile, desktop]   # 各平台外壳详细规格见 platforms/<platform>.md
tags: [layout, app-shell, workbench, dashboard, responsive, dual-theme, web, mobile, desktop]
confidence: medium   # layout 规则来自来源;视觉值无参考、为默认回填
coverage:
  visual_reference: false
  viewports: [desktop]
  themes: [light, dark]
  components:
    observed: [app-shell, sidebar, page-header, toolbar, breadcrumb, list, master-detail, settings-tabs, grid-card, fab, drawer, empty-state, skeleton]
    defaulted: [button, icon-button, input, textarea, select, combobox, checkbox, switch, date-picker, badge, table, pagination, kanban, calendar-card, metric-card, chart, avatar, dialog, confirm, search-palette, toast, menu, tooltip, shortcut-help]
  states:
    observed: [default]
    defaulted: [hover, focus-visible, active, disabled, loading, selected, error, empty]
