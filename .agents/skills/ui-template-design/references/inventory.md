# Inventory

仅任务类 `refactor` 预加载本文。`iterate` 不得加载完整 inventory。

扫描 pages / components / layouts / styles / routes，产出 `01-inventory.yaml`：

- 路由 → 建议 Page Type
- 重复组件与重复视觉词汇
- 不一致 Pattern
- unresolved（必须用户收窄）

没有 UX Model 与 inventory（existing）不得进入 Token Freeze。
