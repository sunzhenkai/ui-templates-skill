skill: ui-template-test

problem:
  clean 模式路径 B（从源重建）的四个确定性机制没有被正文预告，编排时每次都要现场诊断、试错、回填：
  adopt 后 architecture-site 误判 existing；capture 要求 graph 在 session source 内 tracked 且与 HEAD
  字节一致（须提交形成 capture-graph commit）；契约收紧后 make test 的失败面系统性集中在
  fixtures pins / bundle 镜像 / 集合 revision / baseline 四处；模板版本晋升后旧 checkpoint 全开需按新契约重基。

evidence:
  - 两次 run 均命中 architecture-site 误判（测试报告偏差节两次记录）
  - SOURCE_GRAPH_NOT_AT_REVISION 硬停止一次，机制还原后才通过（capture-graph commit 三次迭代）
  - 本会话 make test 因 pins/镜像不同步失败 3 轮（counts 52→55→59、FIXED_REVISION 4 次、commit message 差异导致 revision 不确定）
  - 1.3.0→1.3.1→1.3.2 两次重基均走通同一套工件更新序列

pattern:
  路径 B 的编排失败不来自判断错误，而来自确定性机制未前置说明——全部可写成可执行的步骤/检查。

proposed_change:
  仅改 references/clean-mode.md（不动 SKILL.md 门禁与触发）：
  Step 3 路径 B 增 capture-graph commit 机制；Step 4 增 architecture-site 执行顺序与模板重基序列；
  Step 5 增镜像与 pins 级联清单。

expected_improvement:
  路径 B 重跑消除 3 类硬停止与 3 轮试错；clean 模式全程显著减少诊断循环。

risk: low
